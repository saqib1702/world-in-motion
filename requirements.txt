# --- Web layer ---
Flask>=3.0,<4.0
flask-socketio>=5.3,<6.0
simple-websocket>=1.0      # REQUIRED for real WebSockets in async_mode="threading".
                           # Without it flask-socketio silently serves long-polling
                           # only, so the client's websocket upgrade is refused and
                           # the Vite dev proxy reports ECONNABORTED.

# --- Database (MongoDB Atlas) ---
pymongo>=4.8,<5.0
certifi>=2024.7.4          # CA bundle for Atlas TLS (avoids SSL failures on Windows/macOS)
mongomock>=4.3             # in-memory Mongo fallback used when MONGO_URI is unset (tests/local)

# --- LLM (agent reasoning) ---
google-genai>=1.0          # current unified Gemini SDK: `from google import genai`

# --- Ingestion ---
requests>=2.32
APScheduler>=3.10,<4.0

# --- Config ---
python-dotenv>=1.0

# --- Production WSGI server (AWS deploy) ---
gunicorn>=22.0
